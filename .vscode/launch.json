{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Ratings App",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}/src/Server"
        }
    ]
}